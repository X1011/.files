#! /usr/bin/env bash

apt-mark showmanual > ~/.config/package-list.txt

pushd ~/.config/rclone >/dev/null

rclone-opt() {
	rclone --verbose --delete-excluded --copy-links --fast-list --track-renames "$@"
}

(
	rclone-opt sync {,drive:bak/linux/debian}/etc \
		--include-from=etc.include "$@"
	rclone-opt sync {,drive:bak/linux/debian/}~ \
		--filter-from=./~.filter \
		--filter-from=global.filter "$@"

#  ↓ redirect stderr, too, because that's where rclone outputs the verbose logs
) |& tee bak.log \
| egrep --invert-match \
  -e ': Saving new token in config file' \
  -e ': Modify window is 1ms' \
  -e "Can't transfer non file/directory" \
#  -e ': Updated modification time in destination' \

popd >/dev/null
